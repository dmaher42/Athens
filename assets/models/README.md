# Models directory

This folder keeps environmental meshes (for example buildings or vegetation) that still load directly from the asset pipeline.  Character and NPC models now live under `models/` so they can be copied into `public/assets/models` during asset generation instead of being tracked twice in git.
