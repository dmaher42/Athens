# Dev Bootstrap Checklist

1. `npm install`
2. `npm run dev` – confirm the main experience loads without MIME-type errors and the scene renders.
3. Verify the HUD and mini-map toggles function as expected during live reloads.
4. Inspect `window.__AthensAssetBase` in the console to ensure the value/source pair is populated.
