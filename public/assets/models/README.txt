This directory is populated by `npm run generate:assets`.

The static asset generator emits GLB files for tree meshes and copies any
validated NPC models into place. Binary outputs are ignored by Git, so run the
generator before building or serving the site locally.
