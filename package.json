{
  "name": "athens",
  "version": "1.0.0",
  "description": "Ancient Athens web experience",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:clean": "rimraf node_modules/.vite node_modules/.vite-* .vite || true",
    "dev:force": "vite --force --open --port 4173",
    "build": "node ./scripts/generate-favicon.js && vite build && touch dist/.nojekyll",
    "preview": "vite preview",
    "test": "node --test",
    "html:no-ts": "./scripts/check-html-no-ts.sh"
  },
  "dependencies": {
    "cannon": "^0.6.2",
    "stats.js": "^0.17.0",
    "three": "^0.180.0",
    "tone": "^14.7.77"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "vite": "^5.4.0"
  }
}
